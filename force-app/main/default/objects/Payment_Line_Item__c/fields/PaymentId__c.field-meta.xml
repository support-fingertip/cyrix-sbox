<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PaymentId__c</fullName>
    <description>SAP-side parent payment identifier. The /payment REST endpoint reads this on each line and resolves it to the matching Payment__c record (upserted by Payment__c.PaymentId__c) before stamping the master-detail Payment__c lookup. Not unique — a single SAP payment carries many lines, all with the same PaymentId__c.</description>
    <externalId>true</externalId>
    <label>Payment Id</label>
    <length>80</length>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>

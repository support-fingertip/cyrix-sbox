<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Closed_Lost_Requires_Reason</fullName>
    <active>true</active>
    <description>Requires a Lost Reason when Opportunity stage is Closed Lost.</description>
    <errorConditionFormula>AND(
    ISPICKVAL(StageName, 'Closed Lost'),
    ISPICKVAL(Closed_Lost_Reason__c, '')
)</errorConditionFormula>
    <errorDisplayField>Closed_Lost_Reason__c</errorDisplayField>
    <errorMessage>Closed Lost Reason is required when the stage is Closed Lost.</errorMessage>
</ValidationRule>

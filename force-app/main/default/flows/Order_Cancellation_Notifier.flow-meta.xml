<?xml version="1.0" encoding="UTF-8"?>
<!--
    DEPRECATED. Superseded by OrderTrigger + OrderTriggerHandler.
    Status is set to Obsolete so this flow never fires, but the file is
    retained because changedeploy.yml does not support true deletions
    (it uses --source-dir which requires the file to exist).
    To physically remove, run a manual destructiveChanges deploy.
-->
<Flow xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>62.0</apiVersion>
    <description>Deprecated. See OrderTriggerHandler.cls.</description>
    <environments>Default</environments>
    <interviewLabel>Order Cancellation Notifier {!$Flow.CurrentDateTime}</interviewLabel>
    <label>Order Cancellation Notifier</label>
    <processMetadataValues>
        <name>BuilderType</name>
        <value>
            <stringValue>LightningFlowBuilder</stringValue>
        </value>
    </processMetadataValues>
    <processMetadataValues>
        <name>CanvasMode</name>
        <value>
            <stringValue>AUTO_LAYOUT_CANVAS</stringValue>
        </value>
    </processMetadataValues>
    <processType>AutoLaunchedFlow</processType>
    <start>
        <locationX>0</locationX>
        <locationY>0</locationY>
        <filterFormula>false</filterFormula>
        <object>Order</object>
        <recordTriggerType>CreateAndUpdate</recordTriggerType>
        <triggerType>RecordAfterSave</triggerType>
    </start>
    <status>Obsolete</status>
</Flow>
